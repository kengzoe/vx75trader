{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "gunicorn bot:app --bind 0.0.0.0:10000 --timeout 120 --worker-class gthread --threads 2"
  }
}
